# Gateway config for the Envoy example.
# Single binary serves both HTTP (CRUD APIs on :8080) and ExtProc (gRPC on :10000).
extproc:
  port: 10000

engine:
  model_endpoint: http://ollama:11434/v1
  api_key: unused
  max_tokens: 4096
  timeout: 120s

session_store:
  type: sqlite
  dsn: ":memory:"
